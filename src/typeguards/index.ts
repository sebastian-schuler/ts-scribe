import { isDefined } from './is-defined';
import { isNumber } from './is-number';
import { isString } from './is-string';

/**
 * A collection of type guards.
 */
export const TypeGuards = {
  isDefined,
  isNumber,
  isString,
};
